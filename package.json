{
    "name": "brubeckscan",
    "private": "true",
    "scripts": {
        "build": "npm run build --workspaces --if-present"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "dotenv-mono": "^1.3.8"
    }
}
