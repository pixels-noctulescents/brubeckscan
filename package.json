{
    "name": "brubeckscan",
    "private": "true",
    "scripts": {
        "build": "npm run build --workspaces --if-present"
    },
    "workspaces": [
        "packages/*"
    ],
    "dependencies": {
        "dotenv": "^16.0.3"
    }
}
